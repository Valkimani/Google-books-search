import React, { Component } from 'react';
import Logo from "../Logo/Logo";

const Search = () => {
    return (
        <div>
            <Logo/>

            
        </div>
    );
};

export default Search;